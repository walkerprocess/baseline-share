services:
  - type: web
    name: baseline-share
    runtime: python
    plan: free
    buildCommand: "python --version"
    startCommand: "python server.py"
    healthCheckPath: /healthz
    envVars:
      - key: APP_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
